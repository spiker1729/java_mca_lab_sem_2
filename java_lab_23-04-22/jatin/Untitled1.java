   int n = list.length;
   int cmp;
        int temp = 0;
         for(int i=0; i < n; i++){
                int j=i-1;
         MyComparable k=list[i];
                 for(int j=1; j < (n-i); j++){
                 cmp=list[p].compareTo[q];
                          if(j>=0 && cmp == 1){
                                 //swap elements
                                 temp = list[j-1];
                                 list[j-1] =list[j];
                                 list[j] = temp;
                         }

                 }
         }
